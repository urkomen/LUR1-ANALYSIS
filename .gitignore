# Datos — nunca subir a GitHub
data/raw/
data/processed/
# data/models/*.joblib (fuera de production/, generados por el notebook o la
# CLI de entrenamiento) son zona de pruebas y no se versionan. Solo
# data/models/production/ se sube — es el modelo que usa el pipeline, y sin
# él el pipeline no funcionaría en un clon limpio.
data/models/*
!data/models/production/

# Plan de trabajo y documentación interna (solo local)
plan_LUR1.md
documentacion.md
posts/

# Figuras generadas
figures/

# Grafo de conocimiento (generado por graphify)
graphify-out/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Entornos
.env
*.env
.venv/

# Sistema
.DS_Store
Thumbs.db
